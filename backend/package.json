{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "test": "node --check index.js && node --check walletconnect-pay.js && node --check initialize-wallet.js && node --check solidity-payments.js && node --check deploy-registry.js",
    "deploy:registry": "node deploy-registry.js",
    "deploy:invoices": "node src/deploy-invoice-registry.js"
  },
  "dependencies": {
    "@walletconnect/pay": "^1.0.8",
    "dotenv": "^17.4.0",
    "ethers": "^6.16.0",
    "express": "^5.2.1",
    "solc": "^0.8.30"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
